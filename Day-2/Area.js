// WAP to find area of triangle

const base = 12;
const height = 6;
let area = 0.5 * base * height;

console.log("Given base value : " + base + " & height value : " + height);
console.log("Area of triangle : " + area);
